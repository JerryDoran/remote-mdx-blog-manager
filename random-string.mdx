---
title: 'Create a random string key'
subtitle: 'The node terminal is used to enter the node environment in which you can write a bunch of Javascript.'
date: '2023-06-23'
tags: ['next.js', 'nextjs']
---

<Video id='6h649f2fB9Q' />

Type node in the terminal to enter the node environment

Enter

```js
const randomString = require('crypto').randomBytes(16).toString('hex');

console.log(randomString);
```

You can enter 32 or 64 to make a larger string value
